ALTER TABLE `cc_empleados` ADD `activo` BOOLEAN NOT NULL AFTER `apellido_materno`;
ALTER TABLE `cc_empleados` CHANGE `hora_ingresa` `hora_ingreso` TIME NOT NULL;
ALTER TABLE `cc_users` ADD `activo` BOOLEAN NOT NULL AFTER `color_usuario`;
ALTER TABLE `cc_users` ADD `fecha_acceso` DATE NULL AFTER `activo`, ADD `hora_acceso` TIME NULL AFTER `fecha_acceso`;
update `cc_users` set hora_acceso = '00:00:00', fecha_acceso = '0000-00-00'


INSERT INTO `cc_claves` (`nombre_clave`, `clave`, `descripcion`, `descripcion_corta`, `id_usuario`, `fecha_ingreso`, `hora_ingreso`, `id_usuario_act`, `fecha_act`, `hora_act`) VALUES ('ROL', '1', 'ADMINISTRADOR', 'ADMIN', '1', '2023-07-21', '11:47:05', '0', '0000-00-00', '00:00:00');
INSERT INTO `cc_claves` (`nombre_clave`, `clave`, `descripcion`, `descripcion_corta`, `id_usuario`, `fecha_ingreso`, `hora_ingreso`, `id_usuario_act`, `fecha_act`, `hora_act`) VALUES ('ROL', '2', 'GERENTE', 'GER', '1', '2023-07-21', '11:47:05', '0', '0000-00-00', '00:00:00');
INSERT INTO `cc_claves` (`nombre_clave`, `clave`, `descripcion`, `descripcion_corta`, `id_usuario`, `fecha_ingreso`, `hora_ingreso`, `id_usuario_act`, `fecha_act`, `hora_act`) VALUES ('ROL', '3', 'CAJERO', 'CAJ', '1', '2023-07-21', '11:47:05', '0', '0000-00-00', '00:00:00');

ALTER TABLE `cc_users` CHANGE `fecha_acceso` `fecha_acceso` DATE NOT NULL, CHANGE `hora_acceso` `hora_acceso` TIME NOT NULL;




sudo cp 000-default.conf abstrings.conf
sudo nano abstrings.conf

<VirtualHost *:80>
	# The ServerName directive sets the request scheme, hostname and port that
	# the server uses to identify itself. This is used when creating
	# redirection URLs. In the context of virtual hosts, the ServerName
	# specifies what hostname must appear in the request's Host: header to
	# match this virtual host. For the default virtual host (this file) this
	# value is not decisive as it is used as a last resort host regardless.
	# However, you must set it for any further virtual host explicitly.
	#ServerName www.example.com

	ServerAdmin webmaster@localhost
	ServerName abstrings.com
    ServerAlias www.abstrings.com
    DocumentRoot /var/www/abstrings

	# Available loglevels: trace8, ..., trace1, debug, info, notice, warn,
	# error, crit, alert, emerg.
	# It is also possible to configure the loglevel for particular
	# modules, e.g.
	#LogLevel info ssl:warn

    ErrorLog ${APACHE_LOG_DIR}/abstrings_error.log
    CustomLog ${APACHE_LOG_DIR}/abstrings_access.log combined

	# For most configuration files from conf-available/, which are
	# enabled or disabled at a global level, it is possible to
	# include a line for only one particular virtual host. For example the
	# following line enables the CGI configuration for this host only
	# after it has been globally disabled with "a2disconf".
	#Include conf-available/serve-cgi-bin.conf
	<Directory /var/www/abstrings>
        Options -Indexes +FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>
</VirtualHost>

sudo a2ensite abstrings
sudo service apache2 restart

Contraseña de pair Mu$ic4G4ng4


CREATE USER 'newuser'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON * . * TO 'newuser'@'localhost';
FLUSH PRIVILEGES;


Para filezilla
sftp://34.121.229.39
usuario designer2 Se configura en Metadatos
Sin password



cambiar puerto
